# ============================================================
# UNIT TEST CONFIG - GoogleTest-specific thresholds
#
# Mirror of etc/clang_tidy/src.yml with relaxed function
# size/complexity thresholds. Self-contained because the
# workflow uses explicit -config-file= per scope.
#
# MAINTENANCE: when checks or naming rules change in src.yml,
# update this file to match. The ONLY intentional differences
# are the thresholds marked "TEST OVERRIDE".
# ============================================================
---
CheckOptions:
  # misc-include-cleaner runs whole here as in etc/clang_tidy/src.yml, where the
  # comment states why nothing is ignored: from 2026-09-18 every test file
  # includes the header that declares each symbol it uses and no header it uses
  # nothing from, with vpi_user.h's include of vpi_compatibility.h the one
  # include a file relies on, because the source Annex K.2 prints has
  # vpi_user.h include that file and Annex L forbids including it directly.
  readability-function-cognitive-complexity.IgnoreMacros: true
  readability-function-cognitive-complexity.Threshold: 15
  # TEST OVERRIDE (src: 200)
  readability-function-size.LineThreshold: 300
  readability-function-size.NestingThreshold: 4
  readability-function-size.ParameterThreshold: 5
  # TEST OVERRIDE (src: 50)
  readability-function-size.StatementThreshold: 100
  readability-identifier-naming.ClassCase: CamelCase
  readability-identifier-naming.ClassConstantCase: CamelCase
  readability-identifier-naming.ClassConstantPrefix: k
  readability-identifier-naming.ConstantCase: CamelCase
  # A parameter declared const, as IEEE 1800-2023 Annex I.3 declares every
  # svScope and svOpenArrayHandle parameter of svdpi.h, is a parameter still;
  # left unset, the check would read it as a constant and ask for kCamelCase
  readability-identifier-naming.ConstantParameterCase: lower_case
  readability-identifier-naming.ConstantPrefix: k
  readability-identifier-naming.ConstexprVariableCase: CamelCase
  readability-identifier-naming.ConstexprVariablePrefix: k
  readability-identifier-naming.EnumCase: CamelCase
  readability-identifier-naming.EnumConstantCase: CamelCase
  readability-identifier-naming.EnumConstantPrefix: k
  readability-identifier-naming.FunctionCase: CamelCase
  # C++ standard: main, coroutine promise/awaitable methods
  # IEEE 1800-2023: the 41 vpi_ routines Annex K.2 declares on printed pages
  # 1319 to 1321, in the order printed; vpi_register_assertion_cb, which Annex
  # M declares; the 84 compatibility-mode variants Annex L.2's macros name on
  # printed pages 1323 to 1325, vpi_compare_objects_1364v1995 through
  # vpi_scan_1800v2012, fourteen routines under each of six versions, which
  # src/simulator/vpi_compatibility.cpp defines; the 66 sv* functions Annex
  # I.3 declares on printed pages 1294 to 1298; and the 17 functions of the
  # deprecated portion Annex I.3 prints on pages 1299 to 1300 that
  # src/simulator/svdpi_sv31a.h provides, the sixteen open-array Vec32
  # functions of page 1300 being not provided and so not named. Each group is
  # in the order printed. §H.2 says svdpi.h defines all of the interface's
  # functions, so the names stand here one by one and no pattern stands in
  # for them.
  # The value is one parenthesized alternation because the check wraps it as
  # ^…$ before matching: unparenthesized, the anchors bind to the first and
  # last alternatives alone and every alternative between them matches as a
  # substring, which is how vpi_get64 passed on vpi_get before every routine
  # was named here.
  readability-identifier-naming.FunctionIgnoredRegexp: '(main|get_return_object|initial_suspend|final_suspend|return_void|return_value|unhandled_exception|await_ready|await_suspend|await_resume|vpi_register_cb|vpi_remove_cb|vpi_get_cb_info|vpi_register_systf|vpi_get_systf_info|vpi_handle_by_name|vpi_handle_by_index|vpi_handle|vpi_handle_multi|vpi_iterate|vpi_scan|vpi_get|vpi_get64|vpi_get_str|vpi_get_delays|vpi_put_delays|vpi_get_value|vpi_put_value|vpi_get_value_array|vpi_put_value_array|vpi_get_time|vpi_mcd_open|vpi_mcd_close|vpi_mcd_name|vpi_mcd_printf|vpi_printf|vpi_compare_objects|vpi_chk_error|vpi_free_object|vpi_release_handle|vpi_get_vlog_info|vpi_get_data|vpi_put_data|vpi_get_userdata|vpi_put_userdata|vpi_vprintf|vpi_mcd_vprintf|vpi_flush|vpi_mcd_flush|vpi_control|vpi_handle_by_multi_index|vpi_register_assertion_cb|vpi_compare_objects_1364v1995|vpi_control_1364v1995|vpi_get_1364v1995|vpi_get_str_1364v1995|vpi_get_value_1364v1995|vpi_handle_1364v1995|vpi_handle_by_index_1364v1995|vpi_handle_by_multi_index_1364v1995|vpi_handle_by_name_1364v1995|vpi_handle_multi_1364v1995|vpi_iterate_1364v1995|vpi_put_value_1364v1995|vpi_register_cb_1364v1995|vpi_scan_1364v1995|vpi_compare_objects_1364v2001|vpi_control_1364v2001|vpi_get_1364v2001|vpi_get_str_1364v2001|vpi_get_value_1364v2001|vpi_handle_1364v2001|vpi_handle_by_index_1364v2001|vpi_handle_by_multi_index_1364v2001|vpi_handle_by_name_1364v2001|vpi_handle_multi_1364v2001|vpi_iterate_1364v2001|vpi_put_value_1364v2001|vpi_register_cb_1364v2001|vpi_scan_1364v2001|vpi_compare_objects_1364v2005|vpi_control_1364v2005|vpi_get_1364v2005|vpi_get_str_1364v2005|vpi_get_value_1364v2005|vpi_handle_1364v2005|vpi_handle_by_index_1364v2005|vpi_handle_by_multi_index_1364v2005|vpi_handle_by_name_1364v2005|vpi_handle_multi_1364v2005|vpi_iterate_1364v2005|vpi_put_value_1364v2005|vpi_register_cb_1364v2005|vpi_scan_1364v2005|vpi_compare_objects_1800v2005|vpi_control_1800v2005|vpi_get_1800v2005|vpi_get_str_1800v2005|vpi_get_value_1800v2005|vpi_handle_1800v2005|vpi_handle_by_index_1800v2005|vpi_handle_by_multi_index_1800v2005|vpi_handle_by_name_1800v2005|vpi_handle_multi_1800v2005|vpi_iterate_1800v2005|vpi_put_value_1800v2005|vpi_register_cb_1800v2005|vpi_scan_1800v2005|vpi_compare_objects_1800v2009|vpi_control_1800v2009|vpi_get_1800v2009|vpi_get_str_1800v2009|vpi_get_value_1800v2009|vpi_handle_1800v2009|vpi_handle_by_index_1800v2009|vpi_handle_by_multi_index_1800v2009|vpi_handle_by_name_1800v2009|vpi_handle_multi_1800v2009|vpi_iterate_1800v2009|vpi_put_value_1800v2009|vpi_register_cb_1800v2009|vpi_scan_1800v2009|vpi_compare_objects_1800v2012|vpi_control_1800v2012|vpi_get_1800v2012|vpi_get_str_1800v2012|vpi_get_value_1800v2012|vpi_handle_1800v2012|vpi_handle_by_index_1800v2012|vpi_handle_by_multi_index_1800v2012|vpi_handle_by_name_1800v2012|vpi_handle_multi_1800v2012|vpi_iterate_1800v2012|vpi_put_value_1800v2012|vpi_register_cb_1800v2012|vpi_scan_1800v2012|svDpiVersion|svGetBitselBit|svGetBitselLogic|svPutBitselBit|svPutBitselLogic|svGetPartselBit|svGetPartselLogic|svPutPartselBit|svPutPartselLogic|svLeft|svRight|svLow|svHigh|svIncrement|svSize|svDimensions|svGetArrayPtr|svSizeOfArray|svGetArrElemPtr|svGetArrElemPtr1|svGetArrElemPtr2|svGetArrElemPtr3|svPutBitArrElemVecVal|svPutBitArrElem1VecVal|svPutBitArrElem2VecVal|svPutBitArrElem3VecVal|svPutLogicArrElemVecVal|svPutLogicArrElem1VecVal|svPutLogicArrElem2VecVal|svPutLogicArrElem3VecVal|svGetBitArrElemVecVal|svGetBitArrElem1VecVal|svGetBitArrElem2VecVal|svGetBitArrElem3VecVal|svGetLogicArrElemVecVal|svGetLogicArrElem1VecVal|svGetLogicArrElem2VecVal|svGetLogicArrElem3VecVal|svGetBitArrElem|svGetBitArrElem1|svGetBitArrElem2|svGetBitArrElem3|svGetLogicArrElem|svGetLogicArrElem1|svGetLogicArrElem2|svGetLogicArrElem3|svPutLogicArrElem|svPutLogicArrElem1|svPutLogicArrElem2|svPutLogicArrElem3|svPutBitArrElem|svPutBitArrElem1|svPutBitArrElem2|svPutBitArrElem3|svGetScope|svSetScope|svGetNameFromScope|svGetScopeFromName|svPutUserData|svGetUserData|svGetCallerInfo|svIsDisabledState|svAckDisabledState|svGetTime|svGetTimeUnit|svGetTimePrecision|svSizeOfBitPackedArr|svSizeOfLogicPackedArr|svPutBitVec32|svPutLogicVec32|svGetBitVec32|svGetLogicVec32|svGetSelectBit|svGetSelectLogic|svPutSelectBit|svPutSelectLogic|svGetPartSelectBit|svGetBits|svGet32Bits|svGet64Bits|svGetPartSelectLogic|svPutPartSelectBit|svPutPartSelectLogic)'
  readability-identifier-naming.GlobalConstantCase: CamelCase
  readability-identifier-naming.GlobalConstantPrefix: k
  readability-identifier-naming.LocalConstantCase: CamelCase
  readability-identifier-naming.LocalConstantPrefix: k
  readability-identifier-naming.MethodCase: CamelCase
  # C++ standard: coroutine promise/awaitable + container concept methods.
  # Parenthesized for the reason FunctionIgnoredRegexp's comment gives: bare,
  # the check's ^…$ anchors bind to the first and last alternatives alone,
  # and an append_item passed on end.
  readability-identifier-naming.MethodIgnoredRegexp: '(get_return_object|initial_suspend|final_suspend|return_void|return_value|unhandled_exception|await_ready|await_suspend|await_resume|push_back|begin|end|size|empty|data)'
  readability-identifier-naming.NamespaceCase: lower_case
  readability-identifier-naming.ParameterCase: lower_case
  # IEEE 1800-2023: the parameter names the prototypes of vpi_user.h and
  # svdpi.h carry as the annexes print them, each in the order printed: the
  # six of Annex K.2, refHandle, refHandle1 and refHandle2 on printed page
  # 1320 and fileName, dataLoc and numOfBytes on page 1321, then the four of
  # Annex I.3, scopeName, userKey and userData on page 1297 and lineNumber on
  # page 1298, where fileName is printed a second time. Parenthesized for the
  # reason FunctionIgnoredRegexp's comment gives.
  readability-identifier-naming.ParameterIgnoredRegexp: '(refHandle|refHandle1|refHandle2|fileName|dataLoc|numOfBytes|scopeName|userKey|userData|lineNumber)'
  readability-identifier-naming.PrivateMemberCase: lower_case
  readability-identifier-naming.PrivateMemberSuffix: '_'
  readability-identifier-naming.ProtectedMemberCase: lower_case
  readability-identifier-naming.ProtectedMemberSuffix: '_'
  readability-identifier-naming.PublicMemberCase: lower_case
  # IEEE 1800-2023 Annex M.2: the members of s_vpi_assertion_step_info and
  # s_vpi_attempt_info as the annex spells them on printed page 1335, in the
  # order printed. Parenthesized for the reason FunctionIgnoredRegexp's
  # comment gives.
  readability-identifier-naming.PublicMemberIgnoredRegexp: '(stateFrom|stateTo|failExpr|attemptStartTime)'
  readability-identifier-naming.ScopedEnumConstantCase: CamelCase
  readability-identifier-naming.ScopedEnumConstantPrefix: k
  readability-identifier-naming.StaticConstantCase: CamelCase
  readability-identifier-naming.StaticConstantPrefix: k
  readability-identifier-naming.StructCase: CamelCase
  # C++23 coroutine promise_type [coroutine.handle]
  # IEEE 1800-2023: every struct tag the C API's include files print, each in
  # the order printed: the ten of Annex K.2's vpi_user.h, t_vpi_time and
  # t_vpi_delay on printed page 1315, t_vpi_vecval, t_vpi_strengthval,
  # t_vpi_value and t_vpi_arrayvalue on pages 1315 to 1316, t_vpi_systf_data,
  # t_vpi_vlog_info, t_vpi_error_info and t_cb_data on page 1318; and the two
  # of Annex M.2's sv_vpi_user.h, t_vpi_assertion_step_info and
  # t_vpi_attempt_info on page 1335. Annex I.3's svdpi.h repeats t_vpi_vecval
  # and t_vpi_time on pages 1293 to 1294 and prints no tag of its own, and
  # Annex L.2's vpi_compatibility.h prints none. Parenthesized for the reason
  # FunctionIgnoredRegexp's comment gives.
  readability-identifier-naming.StructIgnoredRegexp: '(promise_type|t_vpi_time|t_vpi_delay|t_vpi_vecval|t_vpi_strengthval|t_vpi_value|t_vpi_arrayvalue|t_vpi_systf_data|t_vpi_vlog_info|t_vpi_error_info|t_cb_data|t_vpi_assertion_step_info|t_vpi_attempt_info)'
  readability-identifier-naming.TemplateParameterCase: CamelCase
  readability-identifier-naming.TypeAliasCase: CamelCase
  readability-identifier-naming.TypedefCase: CamelCase
  # IEEE 1800-2023: every typedef name the C API's include files print, each
  # in the order printed. Annex K.2's vpi_user.h: the six sized types of the
  # portability help's _MSC_VER branch, uint64_t through int8_t, on printed
  # page 1306; PLI_INT64 through PLI_UBYTE8 on page 1307; vpiHandle on page
  # 1308; the s_vpi_ and p_vpi_ names of each struct, s_vpi_time and
  # p_vpi_time through p_vpi_error_info, on pages 1315 to 1318; and
  # s_cb_data and p_cb_data on page 1319. Annex M.2's sv_vpi_user.h:
  # s_vpi_assertion_step_info through p_vpi_attempt_info and
  # vpi_assertion_callback_func on page 1335. Annex I.3's svdpi.h: svScalar,
  # svBit, svLogic, svLogicVecVal and svBitVecVal on page 1293, svTimeVal,
  # svScope and svOpenArrayHandle on page 1294, and, in the deprecated
  # portion, svBitVec32, svLogicVec32, svBitPackedArrRef and
  # svLogicPackedArrRef on page 1299; its sized types, s_vpi_vecval,
  # p_vpi_vecval, s_vpi_time and p_vpi_time repeat Annex K.2's. Annex L.2's
  # vpi_compatibility.h prints none. Parenthesized for the reason
  # FunctionIgnoredRegexp's comment gives.
  readability-identifier-naming.TypedefIgnoredRegexp: '(uint64_t|uint32_t|uint8_t|int64_t|int32_t|int8_t|PLI_INT64|PLI_UINT64|PLI_INT32|PLI_UINT32|PLI_INT16|PLI_UINT16|PLI_BYTE8|PLI_UBYTE8|vpiHandle|s_vpi_time|p_vpi_time|s_vpi_delay|p_vpi_delay|s_vpi_vecval|p_vpi_vecval|s_vpi_strengthval|p_vpi_strengthval|s_vpi_value|p_vpi_value|s_vpi_arrayvalue|p_vpi_arrayvalue|s_vpi_systf_data|p_vpi_systf_data|s_vpi_vlog_info|p_vpi_vlog_info|s_vpi_error_info|p_vpi_error_info|s_cb_data|p_cb_data|s_vpi_assertion_step_info|p_vpi_assertion_step_info|s_vpi_attempt_info|p_vpi_attempt_info|vpi_assertion_callback_func|svScalar|svBit|svLogic|svLogicVecVal|svBitVecVal|svTimeVal|svScope|svOpenArrayHandle|svBitVec32|svLogicVec32|svBitPackedArrRef|svLogicPackedArrRef)'
  readability-identifier-naming.VariableCase: lower_case
# bugprone-random-generator-seed disabled: IEEE 1800-2023 18.13/18.14
# randomization tests use fixed seeds on purpose to assert reproducible
# sequences; the check demands an unpredictable seed, which would defeat them.
# bugprone-incorrect-roundings disabled: IEEE 1800-2023 Annex N.2 rounds every
# distribution draw with (long)(r + 0.5), and the golden transcription the
# Annex N tests hold the simulator to writes the rounding as the annex does.
Checks: >-
  -*,
  bugprone-*,
  -bugprone-easily-swappable-parameters,
  -bugprone-random-generator-seed,
  -bugprone-incorrect-roundings,
  cppcoreguidelines-avoid-goto,
  cppcoreguidelines-init-variables,
  cppcoreguidelines-slicing,
  misc-include-cleaner,
  misc-redundant-expression,
  misc-unused-parameters,
  modernize-use-auto,
  modernize-use-nullptr,
  modernize-use-override,
  performance-*,
  readability-function-cognitive-complexity,
  readability-function-size,
  readability-identifier-naming,
  readability-redundant-smartptr-get,
  readability-simplify-boolean-expr,
  clang-diagnostic-unused-function
# The six files the standard prints and this repository transcribes --
# src/simulator/vpi_user.h (Annex K.2), sv_vpi_user.h (M.2),
# vpi_compatibility.h (L.2), svdpi.h (I.3), svdpi_src.h (H.14.3) and
# svdpi_sv31a.h (H.14.2) -- carry the annex's text and are not rewritten for a
# check's sake: d724ab738 had written vpiUndefined as (-1) for
# bugprone-macro-parentheses where K.2 prints -1. A diagnostic inside one of
# them is therefore not reported; assert-lrm-mandated-files in
# .github/workflows/deltahdl.yml is what holds their text to the annex.
ExcludeHeaderFilterRegex: 'src/simulator/(vpi_user|sv_vpi_user|vpi_compatibility|svdpi|svdpi_src|svdpi_sv31a)\.h$'
# lib/cpp is named because the fixtures, helpers and models every unit test is
# built on live there and nothing else reads them. A diagnostic raised inside a
# header is reported only where the header's path matches this expression, and
# lib/cpp/test_fixtures/fixture_simulator.h has no src/ or test/ component in it
# -- test_fixtures/ is not test/ -- so before lib/cpp was named here every
# diagnostic in those 131 headers was suppressed, in this configuration and in
# etc/clang_tidy/src.yml alike. assert-no-oversized-source-files already reads
# the tree, running `find src test lib/cpp`, so the repository holds lib/cpp to a
# standard and clang-tidy was the one gate skipping it.
#
# This configuration is the one that governs them, and etc/clang_tidy/src.yml
# deliberately does not name lib/cpp. lib/cpp is test-tier code, so the
# thresholds written for that tier are the ones it answers to; and no .cpp under
# src/ includes a lib/cpp header, so naming it there would check nothing.
#
# test/ matches no header today, the test tree holding none. It stays because a
# header added under test/src/unit/ is to be checked from the day it is written.
HeaderFilterRegex: '(src|test|lib/cpp)/.*\.h$'
