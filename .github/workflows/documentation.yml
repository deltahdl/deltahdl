---
concurrency:
  cancel-in-progress: true
  group: documentation-${{ github.ref }}
jobs:
  concluding-jsonlint-necessary:
    if: ${{ needs.determining-jsonlint.outputs.jsonlint-necessary == 'true' }}
    needs: determining-jsonlint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  concluding-jsonlint-unnecessary:
    if: ${{ needs.determining-jsonlint.outputs.jsonlint-necessary == 'false' }}
    needs: determining-jsonlint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  concluding-markdownlint-necessary:
    if: ${{ needs.determining-markdownlint.outputs.markdownlint-necessary == 'true' }}
    needs: determining-markdownlint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  concluding-markdownlint-unnecessary:
    if: ${{ needs.determining-markdownlint.outputs.markdownlint-necessary == 'false' }}
    needs: determining-markdownlint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  concluding-yamllint-necessary:
    if: ${{ needs.determining-yamllint.outputs.yamllint-necessary == 'true' }}
    needs: determining-yamllint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  concluding-yamllint-unnecessary:
    if: ${{ needs.determining-yamllint.outputs.yamllint-necessary == 'false' }}
    needs: determining-yamllint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  determining-changes:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: List the files this push changed
        run: |
          base='${{ github.event.before }}'
          head='${{ github.sha }}'
          zero='0000000000000000000000000000000000000000'
          if [ -z "$base" ] || [ "$base" = "$zero" ] \
              || ! git cat-file -e "$base^{commit}" 2>/dev/null; then
            git ls-files | tee changed-files.txt
          else
            git diff --name-only "$base" "$head" | tee changed-files.txt
          fi
      - name: Publish the list of changed files
        uses: actions/upload-artifact@v4
        with:
          name: changed-files
          path: changed-files.txt
  determining-jsonlint:
    needs: determining-changes
    outputs:
      jsonlint-necessary: ${{ steps.decide.outputs.jsonlint-necessary }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: changed-files
      - id: decide
        name: Decide whether JSON linting is necessary
        run: |
          PATTERN='^docs/.*\.json$|^\.github/workflows/documentation\.yml$'
          if grep -qE "$PATTERN" changed-files.txt; then
            necessary=true
          else
            necessary=false
          fi
          echo "jsonlint-necessary=$necessary" | tee -a "$GITHUB_OUTPUT"
  determining-markdownlint:
    needs: determining-changes
    outputs:
      markdownlint-necessary: >-
        ${{ steps.decide.outputs.markdownlint-necessary }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: changed-files
      - id: decide
        name: Decide whether Markdown linting is necessary
        run: |
          PATTERN='\.md$|^\.github/workflows/documentation\.yml$'
          if grep -qE "$PATTERN" changed-files.txt; then
            necessary=true
          else
            necessary=false
          fi
          echo "markdownlint-necessary=$necessary" | tee -a "$GITHUB_OUTPUT"
  determining-yamllint:
    needs: determining-changes
    outputs:
      yamllint-necessary: ${{ steps.decide.outputs.yamllint-necessary }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: changed-files
      - id: decide
        name: Decide whether YAML linting is necessary
        run: |
          PATTERN='^\.github/workflows/documentation\.yml$'
          if grep -qE "$PATTERN" changed-files.txt; then
            necessary=true
          else
            necessary=false
          fi
          echo "yamllint-necessary=$necessary" | tee -a "$GITHUB_OUTPUT"
  ending:
    if: ${{ !failure() && !cancelled() }}
    needs:
      - jsonlint
      - markdownlint
      - yamllint
    runs-on: ubuntu-24.04
    steps:
      - name: No-op
        run: 'true'
  jsonlint:
    needs: concluding-jsonlint-necessary
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: npm install -g jsonlint
      - name: Lint JSON
        run: |
          find docs -name '*.json' -print0 \
            | xargs -0 -n1 jsonlint --quiet
  markdownlint:
    needs: concluding-markdownlint-necessary
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Restore pip cache
        uses: actions/cache@v5
        with:
          key: >-
            pip-${{ github.job }}-${{
            hashFiles('.github/workflows/documentation.yml') }}
          path: ~/.cache/pip
          restore-keys: |
            pip-${{ github.job }}-
      - name: Install tools
        run: |
          npm install -g markdownlint-cli
          pip install \
            assert-no-inline-directives \
            assert-no-linter-config-files
      - name: Assert no inline markdownlint disables
        run: >-
          assert-no-inline-directives
          --tools markdownlint
          --verbose
          ${{ github.workspace }}/**/*
      - name: Assert no markdownlint config files
        run: >-
          assert-no-linter-config-files
          --linters markdownlint
          --verbose
          ${{ github.workspace }}
      - name: Lint Markdown
        run: markdownlint '**/*.md'
  yamllint:
    needs: concluding-yamllint-necessary
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Restore pip cache
        uses: actions/cache@v5
        with:
          key: >-
            pip-${{ github.job }}-${{
            hashFiles('.github/workflows/documentation.yml') }}
          path: ~/.cache/pip
          restore-keys: |
            pip-${{ github.job }}-
      - name: Install tools
        run: >-
          pip install
          assert-no-inline-directives
          assert-no-linter-config-files
          yamllint
      - name: Assert no inline yamllint disables
        run: >-
          assert-no-inline-directives
          --tools yamllint
          --verbose
          .github/workflows/documentation.yml
      - name: Lint YAML
        run: |
          EMPTY='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY='key-ordering: enable'
          LL='line-length: disable'
          NL='new-line-at-end-of-file: enable'
          TR='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY, $KEY, $LL, $NL, $TR"
          yamllint --strict \
            --config-data \
            "{extends: default, rules: {$RULES}}" \
            .github/workflows/documentation.yml
name: Documentation
on:
  push:
    branches: [main]
    paths:
      - '**/*.md'
      - docs/**/*.json
      - .github/workflows/documentation.yml
  workflow_dispatch: {}
