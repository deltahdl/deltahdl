---
concurrency:
  cancel-in-progress: true
  group: implement-subclause-${{ github.head_ref || github.ref_name }}
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Restore pip cache
        uses: actions/cache@v4
        with:
          key: >-
            pip-implement-subclause-${{
            hashFiles('.github/workflows/implement_subclause.yml') }}
          path: ~/.cache/pip
          restore-keys: |
            pip-implement-subclause-
      - name: Install tools
        run: |
          pip install \
            assert-no-inline-directives \
            assert-no-linter-config-files \
            assert-one-assert-per-pytest \
            mypy pylint pytest pytest-cov yamllint
          npm install -g jscpd
      - name: Assert no inline directives (YAML)
        run: >-
          assert-no-inline-directives
          --tools yamllint
          --verbose
          ${{ github.workspace }}/**/*
      - name: Assert no linter config files (YAML)
        run: >-
          assert-no-linter-config-files
          --linters yamllint
          --verbose
          ${{ github.workspace }}
      - name: Lint YAML
        run: |
          EMPTY='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY='key-ordering: enable'
          LL='line-length: disable'
          NL='new-line-at-end-of-file: enable'
          TR='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY, $KEY, $LL, $NL, $TR"
          yamllint --strict \
            --config-data \
            "{extends: default, rules: {$RULES}}" \
            .github/workflows/implement_subclause.yml
      - name: Assert no inline lint disables
        run: >-
          assert-no-inline-directives
          --tools pylint,mypy
          --verbose
          lib/lrm/ lib/github/
          test/lib/lrm/ test/lib/github/
          ${{ github.workspace }}/scripts/implement_subclause/**/*
          ${{ github.workspace }}/test/scripts/implement_subclause/**/*
      - name: Assert no linter config files
        run: >-
          assert-no-linter-config-files
          --linters pylint,mypy
          --verbose
          ${{ github.workspace }}
      - name: Assert one assert per pytest
        run: >-
          assert-one-assert-per-pytest
          --verbose
          ${{ github.workspace }}/test/lib/lrm
          ${{ github.workspace }}/test/lib/github
          ${{ github.workspace }}/test/scripts/implement_subclause
      - name: Run pylint on source
        run: >-
          PYTHONPATH=.:scripts python3 -m pylint
          lib/lrm/ lib/github/
          scripts/implement_subclause/
          --fail-on=C,R,W --fail-under=10.0
      - name: Run pylint on tests
        run: >-
          PYTHONPATH=.:scripts:test/scripts/implement_subclause/unit
          python3 -m pylint --recursive=y
          test/lib/lrm/ test/lib/github/
          test/scripts/implement_subclause/
          --fail-on=C,R,W --fail-under=10.0
      - name: Run mypy on source
        run: >-
          PYTHONPATH=.:scripts python3 -m mypy
          lib/lrm/ lib/github/
          scripts/implement_subclause/
      - name: Run mypy on tests
        run: >-
          MYPYPATH=.:scripts:test/scripts/implement_subclause/unit
          python3 -m mypy --explicit-package-bases
          test/lib/lrm/ test/lib/github/
          test/scripts/implement_subclause/
      - name: Detect copy-paste in source
        run: >-
          jscpd --pattern "**/*.py" --threshold 0
          --reporters console
          lib/lrm/ lib/github/
          scripts/implement_subclause/
      - name: Detect copy-paste in tests
        run: >-
          jscpd --pattern "**/*.py" --threshold 0
          --reporters console
          test/lib/lrm/ test/lib/github/
          test/scripts/implement_subclause/
      # DO NOT lower coverage thresholds. Uncovered code is untested code —
      # lowering it hides bugs and lets regressions slip through silently.
      - name: Run lib/github unit tests with 100% coverage gate
        run: >-
          python3 -m pytest test/lib/github/unit/ -v
          --cov=lib.github
          --cov-branch
          --cov-report=term-missing
          --cov-fail-under=100
      - name: Run lib/lrm unit tests with 100% coverage gate
        run: >-
          python3 -m pytest test/lib/lrm/unit/ -v
          --cov=lib.lrm
          --cov-branch
          --cov-report=term-missing
          --cov-fail-under=100
      - name: Run unit tests with 100% coverage gate
        run: >-
          PYTHONPATH=.:scripts python3 -m pytest
          test/scripts/implement_subclause/unit/ -v
          --cov=implement_subclause
          --cov-branch
          --cov-report=term-missing
          --cov-fail-under=100
      - name: Run integration tests
        run: >-
          PYTHONPATH=.:scripts python3 -m pytest
          test/scripts/implement_subclause/integration/ -v
      - name: Run e2e tests
        run: >-
          PYTHONPATH=.:scripts python3 -m pytest
          test/scripts/implement_subclause/e2e/ -v
name: implement_subclause
on:
  pull_request:
    paths:
      - .github/workflows/implement_subclause.yml
      - lib/github/**
      - lib/lrm/**
      - scripts/implement_subclause/**
      - test/lib/github/**
      - test/lib/lrm/**
      - test/scripts/implement_subclause/**
  push:
    branches: [main]
    paths:
      - .github/workflows/implement_subclause.yml
      - lib/github/**
      - lib/lrm/**
      - scripts/implement_subclause/**
      - test/lib/github/**
      - test/lib/lrm/**
      - test/scripts/implement_subclause/**
  workflow_dispatch:
