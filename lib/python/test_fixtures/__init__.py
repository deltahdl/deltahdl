"""Shared test fixtures.

The fixtures themselves live in the submodules, which is how every
importer names them: ``lib.python.test_fixtures.subprocess_stubs`` and
``lib.python.test_fixtures.lrm_subclause_dependencies``. This file is the
package marker and holds nothing of its own.
"""
